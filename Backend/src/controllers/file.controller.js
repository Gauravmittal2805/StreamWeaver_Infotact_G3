/**
 * Member 1 File Controller Placeholder
 */

async function uploadFile(req, res) {
  res.status(501).json({
    success: false,
    message: "File upload endpoint to be implemented by Member 1"
  });
}

module.exports = {
  uploadFile
};
